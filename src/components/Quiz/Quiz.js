import React from 'react';
import Option from './Option';

const Quiz = (qs) => {
    const { key, options, question, correct } = qs;
    return (
        <div>
            <h3>{question}</h3>
            
            
        </div>
    );
};

export default Quiz;